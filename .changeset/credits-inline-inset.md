---
"@braccato/core": patch
---

Line the songwriter credits up with the lyrics. The credits took their smaller size on the element the lines' `0.25em` inset applies to, so they sat a few pixels further out than the text above them; the size now lives on an inner `.blyrics-credits__text` block.
