---
"@braccato/core": minor
---

Explicit words are no longer struck through by default. The `blyrics-explicit` class is still set, so a theme that wants the strikethrough can add `.blyrics-explicit { text-decoration: line-through; }`.
