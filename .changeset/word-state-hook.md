---
"@braccato/core": minor
---

Add a per-word `data-word-state` attribute (`upcoming`, `active`, `past`) written on every word, so a theme can style words by whether they are being sung, already sung, or not yet reached.

Fix romanization landing below a translation when the translation arrives first. Romanization now always sits above the translation, regardless of which one is injected first.
