{
  "name": "@braccato/demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "audio": "tsx ../tooling/generate-demo-audio.ts",
    "predev": "pnpm run audio",
    "dev": "vite",
    "prebuild": "pnpm run audio",
    "build": "vite build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@braccato/core": "workspace:*",
    "@braccato/parsers": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.0.7"
  }
}
